# App 5 — SaaS Pricing Page Target
Simulates a pricing page with three tiers in distinct containers.
Port: 3014
Endpoints:
- GET / : View pricing tiers
- POST /update-pro-price : Update Pro plan price (JSON body: { value: number })
