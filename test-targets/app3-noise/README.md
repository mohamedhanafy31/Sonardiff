# App 3 — High Noise Target
Simulates a noisy news homepage with clock, date, and rotating ads.
Port: 3012
Endpoints:
- GET / : View page with noise
- POST /update-headline : Update featured headline (JSON body: { headline: string })
